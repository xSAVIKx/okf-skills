// Command okf-enrich reads an OKF bundle's concept docs and uses an
// OpenAI-compatible LLM to generate a Description for concepts that lack one,
// writing it back to the concept's YAML frontmatter.
package main

func main() {}
